package com.sensis.voyager.app.routeview;

public interface FragmentDataChangedListener {
    /**
     * Called when the data being shown on the fragment needs to be changed
     */
    void onDataChanged();
}
